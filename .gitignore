.idea
.vscode/
bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/
nupkgs/
*.nupkg
*.snupkg
# Test artifacts (generated MIDI files) — but keep committed fixtures
test-data/*.mid
!test-data/fixtures/
!test-data/fixtures/*.mid
# Benchmark results
BenchmarkDotNet.Artifacts/
# ANTLR debug files
.antlr/
*.interp
*.tokens
# Test scripts
*.csx

# Python
__pycache__/
*.py[cod]
.venv/

# Python packaging / test caches
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.eggs/

# Python native binaries (built locally)
bindings/python/celeritas/native/*.dll
bindings/python/celeritas/native/*.so
bindings/python/celeritas/native/*.dylib

# DocFX generated output
docs/_site/
docs/api/*.yml
docs/api/.manifest
docs/**/.docfxignore

# Claude Code agent worktrees (transient, never committed)
.claude/worktrees/

# Scratch output from running the documented CLI commands and examples/ from the
# repo root (see README "MIDI Processing" and examples/12). Root-anchored so the
# tracked fixtures under test-data/ are unaffected.
/*.mid
/*.midi
/*.musicxml
/*.mxl

# Coverage output from `dotnet test --collect`
TestResults/
